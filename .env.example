# Copy this file to .env or run `pnpm setup:env`.
# Never commit real passwords or secrets.
DATABASE_URI=postgresql://USER:PASSWORD@localhost:5432/writers_circle
PAYLOAD_SECRET=generate-with-pnpm-setup-env

